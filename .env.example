# Copy to .env for local reference only; do not commit .env.
# The Spring Boot process must receive these as environment variables.

DB_USERNAME=root
DB_PASSWORD=root
JWT_SECRET=replace_with_a_random_secret_of_at_least_32_bytes
