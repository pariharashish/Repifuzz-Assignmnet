# Copy this file to application-local.properties for local profile-based configuration,
# or set the matching environment variables before starting the application.

spring.datasource.url=jdbc:mysql://localhost:3306/repifuzz_db
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
jwt.secret=${JWT_SECRET}
jwt.expirationMs=86400000
